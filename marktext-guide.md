# 文档目的

教新用户如何下载、安装并使用MarkText

# 适用范围

本文档适用于首次安装和使用 MarkText 的普通用户，重点面向需要**快速完成下载、安装并开始基础使用**的读者。当前内容以 **Windows** 平台为主，**macOS 用户**可访问 [MarkText 官方发布页面](https://github.com/marktext/marktext/releases/latest)，下载 `macOS` 版本的安装包（`.dmg` 文件），安装后参考本文的“基础操作”章节继续使用。其他系统的说明将逐步补充。

# 步骤

## 下载与安装

### Windows

#### 推荐方法：使用WinGet安装

WinGet 是微软官方推出的包管理器，在 **Windows 10 1809（2018年10月更新）及更高版本**，以及 **Windows 11** 中通常都已内置。

1. **打开终端（以管理员身份）：**

- 点击“开始”菜单，输入`cmd`或`PowerShell`。
  
- 在搜索结果中，右键点击“命令提示符”或” Windows PowerShell“，选择”**以管理员身份运行**“。
  

2. **确认WinGet已安装：**

- 在打开的窗口中输入以下命令并回车：

```bash
winget --version
```

- 如果显示版本号（如 `v1.5. ...`），说明已安装。如果提示找不到命令，则需要先安装“应用安装程序”。你可以打开 **Microsoft Store**，搜索“**应用安装程序**”并安装。

3. **安装MarkText**

- 在终端中输入以下命令并回车：

```bash
winget install --id=MarkText.MarkText -e
```

- 输入命令后，WinGet 会自动下载并安装，只需等待进度条走完即可。

#### 其他方法：

> **方法一：手动下载并安装**
> 
> 1. **访问官方下载界面：**打开浏览器，访问 MarkText 在 GitHub 上的 [官方发布页面](https://github.com/marktext/marktext/releases/latest)。
![下载界面](./images/下载安装程序)
>   
> 2. **下载安装程序：**在页面上找到适合 Windows 系统的安装包，通常文件名类似 marktext-win-x64-0.19.1-setup.exe 。点击它即可开始下载。
> 
> > **小提示：**如果访问 GitHub 速度较慢，可以尝试使用镜像网站来加速下载。
> 
> 3. **运行安装程序：**下载完成后，找到并双击运行 `.exe` 安装文件。
>   
> 4. **按照向导完成安装：**跟随安装向导的提示进行操作。在此过程中，你可以选择是“**用户级安装**”（仅限当前用户）还是“**系统级安装**”（所有用户可用）。
>   
> 
> 安装完成后，就可以从开始菜单启动 MarkText 了。
> 
> **方法二：使用 Chocolatey 安装**
> 
> Chocolatey 是一个社区驱动的流行包管理器。如果你的系统没有 WinGet，或者习惯使用 Chocolatey，可以选择此方案。
> 
> 1. **安装Chocolatey本身**
>   
> 
> - **以管理员身份**打开 **命令提示符 (CMD)** 或 **PowerShell**。
>   
> - 复制并粘贴以下命令，然后回车：
>   
> 
> ```bash
> Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
> ```
> 
> - 等待安装完成。安装成功后，**需要关闭并重新打开**管理员终端，才能使 Chocolatey 命令生效。
> 
> 2. **验证Chocolatey是否安装成功**
> 
> - 在新打开的终端中输入`choco`并回车。如果没有报错，说明安装成功。
> 
> 3. **安装MarkText**
> 
> - 在终端中输入以下命令并回车：
> 
> ```bash
> choco install marktext
> ```
> 
> - 输入命令后，Chocolatey会自动搜索、下载并安装MarkText，只需等待进度条走完即可。

## 基础操作

> 说明：以下“语言设置”步骤按英文界面描述；完成设置后，后续步骤统一按简体中文界面描述。

### 语言设置

#### 方法一：

启动时，如果出现**语言选择窗口**，执行以下操作：

1. 在弹出的语言列表中，滚动找到并点击选择`简体中文`。
  
2. 点击`OK`或`Confirm`按钮。
  
3. 软件会提示需要重启才能生效，点击`确定`或`Restart`，等待重启即可。
  

#### 方法二：

如果你已进入**软件主界面**，执行以下操作：

1. 点击左上角主菜单（☰），选择 `File` → `Preferences`。
  
2. 在 Preferences 窗口中选择 `General`。
  
3. 在 `General` 页面中找到 `Language` 选项。
  
4. 展开下拉列表，选择 `简体中文`。
  
5. 按提示重启软件，使设置生效。
  

### 创建与保存文档

- **新建文档：**点击左上角主菜单（☰），选择`文件`→`新建标签页`，或使用快捷键`Ctrl+T`创建新的空白文档。
  
- **保存文档：**点击左上角主菜单（☰），选择`文件`→`保存`，或使用快捷键`Ctrl+S`。
  
- **自动保存：**点击左上角主菜单（☰），选择`文件`→`偏好设置`→`通用`→`自动保存`
  

> 注：文件只有在第一次保存后，自动保存功能才会生效。

- **导出文档：**点击左上角主菜单（☰），选择`文件`→`导出`，根据个人需要选择`导出为HTML`或`导出为PDF`。

### 查找与替换

**查找：**

1. 点击左上角主菜单（☰），选择`编辑`→`查找`，或使用快捷键`Ctrl+F`。
  
2. 在页面右上角弹出的查找框中，输入关键词，编辑器会高亮显示所有匹配项。
  
3. 点击查找框右侧上箭头（︿）或使用快捷键`Shift+F3`查找上一项
  
4. 点击查找框右侧下箭头（﹀）或使用快捷键`F3`查找下一项
  

**替换：**

1. 点击左上角主菜单（☰），选择`编辑`→`替换`，或使用快捷键`Ctrl+R`。
  
2. 在替换面板中，分别输入“查找内容”和“替换为”的内容，然后根据个人情况选择“单个替换”或“全部替换”。
  

### 主题设置

- 点击左上角主菜单（☰），选择`文件`→`偏好设置`→`编辑器`，更改编辑器的**字体、行高**。
  
- 点击左上角主菜单（☰），选择`文件`→`偏好设置`→`主题`，选择内置的**亮色或暗色主题**。
  

### 常用快捷键

| 描述  | 组合键 |
| --- | --- |
| 保存  | `Ctrl+S` |
| 复制  | `Ctrl+C` |
| 粘贴  | `Ctrl+V` |
| 撤销  | `Ctrl+Z` |
| 回退  | `Ctrl+Shift+Z` |
| 新建标签页 | `Ctrl+T` |
| 关闭标签页 | `Ctrl+W` |
| 加粗  | `Ctrl+B` |
| 斜体  | `Ctrl+I` |

# 常见问题

- **保存后文件在哪里？**

> 文件保存在你第一次保存时选择的目录中。如果没有手动指定位置，建议在保存时先选择一个固定的工作文件夹。

- **为什么语言设置后没有生效？**

> 语言更改后需要重启软件才能生效。如果重启后仍未生效，请确认是否已成功选择`简体中文`，并检查是否同时打开了多个窗口。

- **新建文档后为什么不能自动保存？**

> 新建文档第一次保存后，自动保存才会生效。请先使用 Ctrl+S 保存一次，并指定文件名和保存位置。

- **导入已有 Markdown 文件怎么做？**

> 可以通过 File → Open 打开已有 .md 文件，也可以将文件直接拖入编辑区。
